// Métodos de Interação

let frutas = ['Banana', 'Maçã', 'Goiaba', 'uva'];

// let bigFrutas = frutas.filter((value) => value.length > 4);

// let check = frutas.every((value) => value.length > 3);
// let check = frutas.some((value) => value.length > 3);


// if (check) {
//    console.log(`Todos são maior do que 3`);
// } else {
//    console.log(`Não são todos maior que 3`);
// }

// Método includes ele verifica se tem o intem dentro do  array.
if (frutas.includes('Melão')) {
   console.log('Tem sim!');
} else {
   console.log('Não tem..');
}

